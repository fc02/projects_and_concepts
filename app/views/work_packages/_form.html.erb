<%= form_for(@work_package) do |f| %>
  <% if @work_package.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_package.errors.count, "error") %> prohibited this work_package from being saved:</h2>

      <ul>
      <% @work_package.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
